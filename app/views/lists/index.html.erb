<h1>投稿一覧</h1>

<% @lists.each do |list| %>

    <p>タイトル</p>
    <spna>
        <%= link_to list.title, list_path(list.id) %>
    </spna>

    <p>画像</p>
    <%= image_tag list.path, size:"200x200" %>

<% end %>
